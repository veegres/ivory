package ssh

import "testing"

func TestExecute(t *testing.T) {
	// NOTE: this is just a placeholder to keep the test file if needed,
	// but we don't have real ssh tests here because they require a server.
}
